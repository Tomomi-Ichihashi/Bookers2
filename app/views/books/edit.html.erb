<div class = "container">
 <div class = "row">
  <div class = "col-md-4 mx-auto mt-3 mb-5">

<h1>Editing Book</h1>
<% if @book.errors.any? %>
 <div class = "error_explanation">
  <h2 class = "text-danger"><%= pluralize(@book.errors.count,"error") %> prohibited this obj from being saved:</h2>
  <ul class = "text-danger">
   <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
   <% end %>
  </ul>
 </div>
<% end %>

 <%= form_with model:@book, local:true do |f| %>
  
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinon</h4>
  <%= f.text_area :body %>
  <br />
  <%= f.submit 'Update Book', class: "btn btn-success btn-sm" %>
  
  <% end %>

<span>
<%= link_to 'Show',  book_path(@book.id) %>
</span>

<span>|</span>

<span>
<%= link_to 'Back', books_path %>
</span>

</div>
</div>
</div>